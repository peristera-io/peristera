# In-cluster control plane: ServiceAccount + least-privilege RBAC +
# Deployment + Service + Ingress. Dev values (image tag, domains, http)
# are opinionated for the k3d cluster; production overlays arrive with M6.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: control-plane
  namespace: peristera-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: peristera-control-plane
rules:
  - apiGroups: ["peristera.io"]
    resources: ["tenants", "tenants/status", "tenants/finalizers"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get", "list", "watch", "create", "delete"]
  # Secrets: get (per-name reads of CNPG creds + own secrets) and create
  # (per-tenant secrets). Deliberately NO list/watch — the SA must not be able
  # to enumerate every secret in the cluster (Zitadel masterkey, system-user
  # key, kube-system) (#7). The controller caches no Secrets and only Gets them
  # by name, so list/watch is unnecessary. A namespace-scoped model (per-tenant
  # Roles) is the fuller MSP-alpha fix.
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create"]
  - apiGroups: ["postgresql.cnpg.io"]
    resources: ["clusters", "databases", "scheduledbackups"]
    verbs: ["get", "list", "watch", "create", "delete"]
  # update: the optional-app reconcile rolls Kamara's Deployment env on an
  # office toggle (R93/#47); create/delete cover provisioning + teardown.
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "watch", "create", "update", "delete"]
  - apiGroups: [""]
    resources: ["services"]
    verbs: ["get", "list", "watch", "create", "delete"]
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["get", "list", "watch", "create", "delete"]
  # networkpolicies also update: the office toggle rewrites np-kamara's caller
  # set (R93/#47). ingresses are only ever created or deleted.
  - apiGroups: ["networking.k8s.io"]
    resources: ["networkpolicies"]
    verbs: ["get", "list", "watch", "create", "update", "delete"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get", "list", "watch", "create", "delete"]
  # cronjobs: the per-tenant blob-backup job (#59/#77), create-only like the
  # rest of provisioning (delete comes with the owner-ref GC on off-boarding).
  # Unused in dev (no backup bucket) but RBAC stays identical to the cloud.
  - apiGroups: ["batch"]
    resources: ["cronjobs"]
    verbs: ["get", "list", "watch", "create", "delete"]
  # (No cert-manager RBAC: DNS-01 issuance (ADR-0021) retired the healTenantCerts
  # self-heal that swept stuck certs; cert-manager's ingress-shim owns them.)
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: peristera-control-plane
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: peristera-control-plane
subjects:
  - kind: ServiceAccount
    name: control-plane
    namespace: peristera-system
---
# Leader election leases (namespaced).
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: control-plane-leases
  namespace: peristera-system
rules:
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: control-plane-leases
  namespace: peristera-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: control-plane-leases
subjects:
  - kind: ServiceAccount
    name: control-plane
    namespace: peristera-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane
  namespace: peristera-system
  labels: {app.kubernetes.io/name: control-plane}
spec:
  replicas: 1
  selector:
    matchLabels: {app.kubernetes.io/name: control-plane}
  template:
    metadata:
      labels: {app.kubernetes.io/name: control-plane}
    spec:
      serviceAccountName: control-plane
      # Non-root, least-privilege posture (#91). The image already runs as
      # uid 65532 (distroless nonroot); this asserts and enforces it, drops all
      # capabilities, and locks the root filesystem read-only. fsGroup keeps any
      # future mounted volume writable by the app group.
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: control-plane
          image: peristera-control-plane:dev
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8090
          env:
            - {name: SYSTEM_USER_KEY, value: /secrets/admin-client/tls.key}
            - {name: ZITADEL_BASE_URL, value: "http://iam.127.0.0.1.sslip.io:9080"}
            - {name: TENANT_BASE_DOMAIN, value: "127.0.0.1.sslip.io"}
            - {name: TENANT_EXTERNAL_PORT, value: "9080"}
            - {name: ZITADEL_EXTERNAL_DOMAIN, value: "iam.127.0.0.1.sslip.io"}
            - {name: CP_LISTEN_ADDR, value: ":8090"}
            - {name: CP_PUBLIC_URL, value: "http://cp.127.0.0.1.sslip.io:9080"}
            # Operator authorization (ADR-0019): the platform OpenFGA + its key,
            # and the bootstrap operator subjects (Zitadel `sub`s in the default
            # instance). OPERATOR_SUBJECTS is set by hack/dev-cluster.sh from the
            # iam-admin user; an empty value locks everyone out.
            - {name: OPENFGA_API_URL, value: "http://cp-openfga.peristera-system.svc.cluster.local:8080"}
            - name: OPENFGA_API_TOKEN
              valueFrom:
                secretKeyRef: {name: cp-openfga-authn-key, key: token}
            - {name: OPERATOR_SUBJECTS, value: ""}
          volumeMounts:
            - {name: admin-client, mountPath: /secrets/admin-client, readOnly: true}
            - {name: tmp, mountPath: /tmp}
          readinessProbe:
            httpGet: {path: /healthz, port: 8090}
      volumes:
        - name: admin-client
          secret:
            secretName: admin-client-tls
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: peristera-system
spec:
  selector: {app.kubernetes.io/name: control-plane}
  ports:
    - {port: 80, targetPort: 8090}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: control-plane
  namespace: peristera-system
  annotations:
    # Must outrank Zitadel's *.127.0.0.1.sslip.io wildcard (tenant
    # issuers), which otherwise swallows this exact host.
    traefik.ingress.kubernetes.io/router.priority: "1000"
spec:
  ingressClassName: traefik
  rules:
    - host: cp.127.0.0.1.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: control-plane
                port: {number: 80}
